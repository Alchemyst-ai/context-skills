# {{PROJECT_NAME}} — Alchemyst agent ({{ARCHETYPE}})

Generated by `contextual-agent-generator` on {{DATE}}.

This skill plans and applies a context-aware AI agent for
**{{PROJECT_NAME}}**, built on the [Alchemyst](https://getalchemystai.com)
context platform.

## Quick start

1. **Read the plan**: open [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md).
2. **Set the API key**: copy `.env.example` to `.env` (in
   `{{PROJECT_PATH}}` or alongside the scaffolds), put your key.
3. **Run the verification**: follow [RUNBOOK.md](RUNBOOK.md) end-to-end.
4. **Apply the changes**:
   - Greenfield: copy `scaffolds/` into your project; `cd` and install.
   - Retrofit: walk the numbered edits in `IMPLEMENTATION_PLAN.md`.

## Files

| File                                                | Purpose                                              |
|-----------------------------------------------------|------------------------------------------------------|
| [SKILL.md](SKILL.md)                                | How Claude drives this skill at invocation time.     |
| [CAPABILITIES.md](CAPABILITIES.md)                  | What the agent must be able to do (interview output).|
| [ARCHITECTURE.md](ARCHITECTURE.md)                  | Archetype, `groupName` design, data flow.            |
| [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)    | Ordered plan ({{MODE}}-specific).                    |
| [RUNBOOK.md](RUNBOOK.md)                            | Verification + ops checks.                           |
| [scaffolds/](scaffolds/)                            | Starter code in `{{SDK}}`.                           |
| [examples/](examples/)                              | Canonical example for the matched archetype.        |
| [custom/](custom/)                                  | Your edits live here; survive regeneration.          |

## Mode

This skill was generated in **`{{MODE}}`** mode.

- **greenfield** — the scaffolds are the starting point of a new project.
- **retrofit** — the scaffolds are reference snippets; the plan is the
  list of edits to apply to your existing codebase.

## SDK

`{{SDK}}` only. If you need the other language, regenerate with the
override.

## Regenerating

If the project's needs have changed materially — new capabilities,
new data sources, the agent now also needs memory, etc. — regenerate
with the parent skill:

> "Regenerate `contextual-agent-generator--{{SKILL_SUFFIX}}`."

Anything under [custom/](custom/) is preserved across regeneration.
